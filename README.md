# playlist-manager-doubly-linked-list
"A terminal-based playlist manager implemeted in C++ using a doubly linked list, supporting song insertion, deletion and bidirectional traversal (next/previous) to simulate a music player."
